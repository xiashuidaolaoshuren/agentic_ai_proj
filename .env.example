# Copy to `.env` and fill in values as you implement later tasks.
# Never commit real secrets.

# --- OpenAI-compatible LLM (summarization; Milestone 1 T8+) ---
OPENAI_API_KEY=
# Optional: default is provider-specific; set for local gateways or Azure-style endpoints.
OPENAI_BASE_URL=

# --- GitHub API (connector; higher rate limits with a token) ---
GITHUB_TOKEN=

# --- Optional overrides (storage layer when implemented) ---
# AI_NEWS_AGENT_DATABASE_PATH=.data/digest.db
